name: listcontrast-preview

services:
  listcontrast-preview:
    build:
      context: ../..
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8080"
    networks:
      web_proxy:
        aliases:
          - listcontrast-preview
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1:8080/"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s

networks:
  web_proxy:
    external: true
    name: web_proxy
